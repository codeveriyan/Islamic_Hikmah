fastapi==0.110.1
uvicorn==0.25.0
cryptography>=42.0.8
python-dotenv>=1.0.1
pymongo==4.6.3
pydantic>=2.6.4
pyjwt>=2.10.1
motor==3.3.1
requests>=2.31.0
python-multipart>=0.0.9
imageio-ffmpeg==0.6.0
# Real Quran ASR. imageio-ffmpeg supplies the decoder when system FFmpeg is absent.
# This PyPI pin is the portable CPU fallback. On an NVIDIA deployment, install
# requirements-asr-gpu.txt after this file so Whisper can use CUDA.
torch==2.12.0
transformers==4.57.6
