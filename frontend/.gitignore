# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
.metro-cache/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files — never commit secrets
.env
.env*.local

# typescript
*.tsbuildinfo

# Content that lives on Cloudflare R2 CDN — never commit these
# Run `node scripts/chunkTafsirs.js` to regenerate locally,
# then `node scripts/uploadToR2.js` to push to the CDN.
public/tafsirs/
public/tafsirs_chunked/

app-example

scratch_*
*.local.json
*.local.txt
